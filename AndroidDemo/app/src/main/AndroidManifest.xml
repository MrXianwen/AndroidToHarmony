<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.hxw.android.harmony">

    <uses-feature
        android:name="zidane.software.ability"
        android:required="false" />
    <application
        android:name=".MyAndroidApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidToHarmony">
        <meta-data
            android:name="permZA"
            android:value="true" />
        <meta-data
            android:name="multiFrameworkBundle"
            android:value="true" />

        <activity
            android:name="cn.hxw.android.harmony.AndroidMainActivity"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>